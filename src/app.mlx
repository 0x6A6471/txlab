open React

let[@react.component] make () =
  let url = ReasonReactRouter.useUrl () in

  <Shared.Layout>
    (match url.path with
     | [] -> <Pages.Home />
     | _ -> <h1>(string "404")</h1>)
  </Shared.Layout>
;;
